apiVersion: v1
kind: Service
metadata:
  name: cost-living-platform-api
  namespace: cost-living
spec:
  selector:
    app: cost-living-platform-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
  type: ClusterIP
